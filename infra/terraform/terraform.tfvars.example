# Copy to terraform.tfvars for a real apply and fill in. rds_password must
# come from a secret store (never commit a real value).
region       = "us-east-1"
name_prefix  = "scriptorium"
# rds_password = "set-via-TF_VAR_rds_password-or-secrets-manager"
