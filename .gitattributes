# Normalize line endings: LF in the repo and working tree (containers and CI are Linux)
* text=auto eol=lf

# Windows scripts keep CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Binaries
*.png binary
*.jpg binary
*.pdf binary
*.jar binary
