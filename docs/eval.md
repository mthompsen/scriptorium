# AI evaluation — methodology and results

Methodology is defined in `docs/DESIGN.md` Section 9.4: retrieval metrics
(recall@k, MRR) on a fixed labeled set, and generation metrics (citation
coverage, LLM-as-judge groundedness with a human-checked subset), regenerated
by the agent service's `/eval/run` endpoint.

## Results

**No eval runs yet.** The eval harness lands in M2 (retrieval metrics) and M3
(generation metrics). Per the working agreement, numbers appear here only from
a real run — never asserted by hand.
